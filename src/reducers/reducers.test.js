import streamReducer from './streamReducer'


test('streamReducer works', () => {
    // Here, I am trying to send an action to the reducer, and
    // I hope to get the object I want.


    const newSate = streamReducer(state, )
})
